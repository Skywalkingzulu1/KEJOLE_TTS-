# Server configuration
PORT=3000

# Path to Google Cloud service account JSON key file.
# Ensure the file is secured and not committed to version control.
GOOGLE_APPLICATION_CREDENTIALS=./path/to/your/service-account.json